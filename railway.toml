[build]
builder = "NIXPACKS"

[deploy]
startCommand = "gunicorn -k eventlet -w 1 --bind 0.0.0.0:$PORT --graceful-timeout 10 --timeout 120 run:app"
healthcheckPath = "/"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
